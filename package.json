{
  "name": "interactive-code-scroll-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter example-oauth-pkce dev",
    "build": "pnpm --filter example-oauth-pkce build",
    "check": "pnpm -r check",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@playwright/test": "1.63.0",
    "typescript": "6.0.3",
    "vitest": "5.0.2"
  }
}
